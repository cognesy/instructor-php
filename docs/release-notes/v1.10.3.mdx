## Changes

### Bug Fixes

- Removed `#[\Override]` attribute from `FlexibleDateDenormalizer::getSupportedTypes()` - method only exists in Symfony 7+ DenormalizerInterface, causing fatal errors with Symfony 6.4.
