# Structured Output UX Contract Plan (Superseded)

Date: 2026-07-20

Epic: `instructor-zlud`

The earlier compatibility-only plan was superseded after direct runtime analysis found that implicit `Structure` resolution can override explicit output targets and that synchronous and streaming materialization have different validation/transformation semantics.

The current 2.5 contract, defect audit, stepping-stone implementation plan, regression suite, and release gates live in:

`research/2.5-cleaner-structured-outputs/`

Start with `research/2.5-cleaner-structured-outputs/README.md`.
