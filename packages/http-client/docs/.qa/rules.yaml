version: 1
rules:
  - id: http.docs.no_provider_type_key
    engine: regex
    scope: markdown
    pattern: '/\bproviderType\b/'
    message: 'HTTP client docs should use `driver` naming, never `providerType`.'

