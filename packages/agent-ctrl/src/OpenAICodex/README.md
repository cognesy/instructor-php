# OpenAICodex Bridge (Internal Module)

This directory contains the OpenAI Codex bridge internals (`Application/*`, `Domain/*`).

Canonical documentation moved to package docs:

- `packages/agent-ctrl/docs/9-codex-bridge.md`

For normal usage, prefer the unified facade:

- `Cognesy\AgentCtrl\AgentCtrl::codex()`
