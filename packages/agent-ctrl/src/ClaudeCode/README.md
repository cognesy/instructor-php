# ClaudeCode Bridge (Internal Module)

This directory contains the Claude Code bridge internals (`Application/*`, `Domain/*`).

Canonical documentation moved to package docs:

- `packages/agent-ctrl/docs/8-claude-code-bridge.md`

For normal usage, prefer the unified facade:

- `Cognesy\AgentCtrl\AgentCtrl::claudeCode()`
