The previous response could not be deserialized into the expected structure.

Return a corrected response that matches the expected schema exactly.

## Invalid Payload

```json
{{ invalid_payload|raw }}
```

## Deserialization Error

{{ error|raw }}

## Expected Schema

```json
{{ json_schema|raw }}
```
