# Overview

Instructor2 keeps Instructor structured output features with a simplified runtime design.

Main classes:

- `StructuredOutput` - fluent request builder
- `StructuredOutputRuntime` - runtime entrypoint
- `PendingStructuredOutput` - executable request handle
- `StructuredOutputStream` - streaming updates
