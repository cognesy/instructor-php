## Bug Fixes

### Test Suite Corrections
- Fixed test failures caused by removal of `Messages::middle()` method in v1.8.0
- Updated test assertions to use alternative message access patterns
- Restored test suite compatibility with new immutable message handling

## Notes

This is a patch release addressing test suite issues introduced in v1.8.0.
No functional changes to the library itself.