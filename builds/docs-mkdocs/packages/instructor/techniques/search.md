---
title: Search
description: 'Turn one request into structured search intent.'
---

Search-style tasks often need the model to break text into fields or a list of query fragments.

Use a small class when one query is enough, or `Sequence` when the result is a list.

The important part is not the prompt complexity. It is choosing a response model that matches how your application will consume the result.
